{
  "name": "minesweeper-ws",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --env dev",
    "deploy:dev": "wrangler deploy --env dev",
    "deploy:prod": "wrangler deploy --env prod",
    "watch:dev": "wrangler tail --env dev",
    "watch:prod": "wrangler tail --env prod"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "wrangler": "^4"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20260111.0"
  }
}
